#include <stdio.h>

int main(void) {
  printf("Gott mit uns\n");

  int identidade = 0;
  ///Chegará aqui por procedures. é o automato

  if (identidade == 1) {
    printf("Gott mit uns\n");
  } else {
    
    printf("stand united with %d\n\n", identidade);

    int iterador = 13, bound = 10;
    
    for (iterador = 2; iterador < bound; iterador++) {
      printf("\t\tstand united with %d\n", iterador);
    }
  }
  return 0;
}